import { StyleSheet } from "react-native"
    
export const styles = StyleSheet.create({
    mapContainer:{
        justifyContent: 'center',
        alignItems:'center',
        marginTop: '20%'
    },
    mapImage:{
        width: 350,
        height: 300,
        borderColor: 'black',
        borderWidth: 0.5
    }
})